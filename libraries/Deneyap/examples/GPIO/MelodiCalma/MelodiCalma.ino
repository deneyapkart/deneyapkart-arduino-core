/*
 *   Daha dün annemizin ninnisinin melodi verilerinin çalıştırılması örneği   
*/
#include "pitches.h"

#define SpeakerPin DAC1 // herhangi bir GPIO pini kullanılabilinir
#define Frequency 500

void setup() {}

void loop() {
  tone(SpeakerPin, NOTE_C5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_C5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_G5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_G5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_A5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_A5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_G5, Frequency);
  noTone(SpeakerPin);

  tone(SpeakerPin, NOTE_F5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_F5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_E5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_E5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_D5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_D5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_C5, Frequency);
  noTone(SpeakerPin);

  tone(SpeakerPin, NOTE_G5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_G5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_F5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_F5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_E5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_E5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_D5, Frequency);
  noTone(SpeakerPin);

  tone(SpeakerPin, NOTE_G5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_G5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_F5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_F5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_E5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_E5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_D5, Frequency);
  noTone(SpeakerPin);  

  tone(SpeakerPin, NOTE_C5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_C5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_G5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_G5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_A5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_A5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_G5, Frequency);
  noTone(SpeakerPin);  

  tone(SpeakerPin, NOTE_F5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_F5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_E5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_E5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_D5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_D5, Frequency);
  noTone(SpeakerPin);
  tone(SpeakerPin, NOTE_C5, Frequency);
  noTone(SpeakerPin);
}